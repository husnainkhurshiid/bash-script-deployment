name: Auto Deployment

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run Deployment Script
        run: |
          chmod +x .github/workflows/automate.bash  # Make the script executable
          ./.github/workflows/automate.bash  # Execute the script
